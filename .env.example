# Copy to .env and fill in, or export directly in your shell.
# Free key: https://fdc.nal.usda.gov/api-key-signup
USDA_API_KEY=DEMO_KEY

# Optional — only needed if 8080 is already taken on your machine
DASHBOARD_PORT=8080
